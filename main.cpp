#include <iostream>
#include <vector>
#include <string>

using namespace std;

int main()
{
  vector <vector <string> > wordGrid;

  /*
     do not edit this space

     where this comment is, the instructor will
     add the code that gets the user's input and inserts the input into every element of the

     matrix wordGrid
  */
   
  //PU
  //that prints each element of the matrix in the
  //following format:
  //Row _ Column _ is _
  //the first and second blanks should be replaced with the coordinates
  //o:wq
  s row and column and the third blank should be replaced
  //with the value of that element
    int max=wordGrid.size();
   
    for (init r=0;r<max;r++)
    {
      for (init c=0;c<max;index++)
      {
        cout<<"Row "<<r<<"Column "<<c<<"is "<<max[r][c];
      }
    }

 return 0;
 }
      
  



 return 0;
}




